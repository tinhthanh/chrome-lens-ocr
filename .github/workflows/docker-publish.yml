# Tên của toàn bộ quy trình
name: Build, Push, and Deploy Docker Image

# Kích hoạt quy trình
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  # === JOB 1: BUILD VÀ PUSH IMAGE LÊN DOCKER HUB (KHÔNG THAY ĐỔI) ===
  build-and-push:
    name: Build and Push to Docker Hub
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/vg-ocr-api:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/vg-ocr-api:${{ github.sha }}

  # === JOB 2: DEPLOY IMAGE LÊN SERVER (PHIÊN BẢN CHÍNH XÁC) ===
  deploy:
    name: Deploy to Production Server
    needs: build-and-push
    runs-on: ubuntu-latest
    
    steps:
      # Bước 1: Checkout code để có thể truy cập file docker-compose.prod.yml
      - name: Checkout repository
        uses: actions/checkout@v3

      # BƯỚC 2: Đọc nội dung file và lưu vào biến môi trường (ĐÃ SỬA LỖI CÚ PHÁP)
      - name: Read docker-compose.prod.yml content
        run: |
          # Dùng cú pháp chuẩn: echo "VARIABLE_NAME<<DELIMITER" >> $GITHUB_ENV
          echo "DOCKER_COMPOSE_CONTENT<<EOF" >> $GITHUB_ENV
          cat docker-compose.prod.yml >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      # Bước 3: SSH vào server và thực thi các lệnh deploy
      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          
          script: |
            echo "🚀 Starting deployment..."
            
            TARGET_DIR="/home/vetgo/cicd/app_services/repo/vg-ocr-api"
            
            echo "Ensuring deployment directory exists at $TARGET_DIR"
            mkdir -p $TARGET_DIR
            
            # Ghi nội dung file từ biến môi trường $DOCKER_COMPOSE_CONTENT
            echo "Writing docker-compose.prod.yml from source code..."
            echo "$DOCKER_COMPOSE_CONTENT" > $TARGET_DIR/docker-compose.prod.yml
            
            cd $TARGET_DIR
            
            export DOCKERHUB_USERNAME=${{ secrets.DOCKERHUB_USERNAME }}
            
            echo " pulling latest image from DockerHub..."
            docker-compose -f docker-compose.prod.yml pull
            
            echo " starting containers with the new image..."
            docker-compose -f docker-compose.prod.yml up -d --remove-orphans
            
            echo " cleaning up old images..."
            docker image prune -f
            
            echo "✅ Deployment finished successfully!"